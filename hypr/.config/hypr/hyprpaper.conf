# Change this path to the wallpaper you want to use.
# An empty monitor value applies it as the fallback for every monitor.
splash = false

wallpaper {
    monitor =
    path = ~/.config/hypr/assets/blank.png
    fit_mode = cover
}
